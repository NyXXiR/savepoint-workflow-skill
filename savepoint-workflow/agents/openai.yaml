interface:
  display_name: "Savepoint Workflow"
  short_description: "Resume work across agents"
  default_prompt: "Use $savepoint-workflow to keep this implementation resumable across agents, sessions, and providers."
